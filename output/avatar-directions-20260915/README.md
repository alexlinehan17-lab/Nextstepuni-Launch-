# Avatar directions

Six illustrated avatar families with four examples each. Generated using OpenAI built-in image generation with existing Nextstepuni artwork as style references.

Open `index.html` through the local development server. Select any example to preview it at drawer, header and profile sizes. Full prompts and source paths are in `prompts.json`.

Exploration only; these assets have not replaced the current avatars.
