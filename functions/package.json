{
  "name": "nextstepuni-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "provision:admin": "node scripts/set-admin-claim.mjs"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  },
  "overrides": {
    "protobufjs": "7.6.5",
    "uuid": "11.1.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
